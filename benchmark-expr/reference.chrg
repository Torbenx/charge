// Open-addressing hash map with Robin Hood probing.

        count;

        count == 0;

        h & (slots.size() - 1);

        limit = slots.size() * MAX_LOAD_PERCENT;
        count * 100 >= limit;

        h = hash_of(key);
        index = slot_index(h);
        probe = 0;

            slot = slots[index];
            slot.state == SlotState::Empty;

            slot.state == SlotState::Occupied && slot.hash == h;
                slot.key == *key;
                    slot.value;

            probe > slot.distance;

            index = slot_index(index + 1);
            probe += 1;

        missing;

        should_grow();
            rehash(slots.size() * 2);

        h = hash_of(key);
        index = slot_index(h);
        distance = 0;
        pending = Slot{K, V}( key, value, h);

            slot = slots[index];
            slot.state != SlotState::Occupied;
                slot.key = pending.key;
                slot.value = pending.value;
                slot.hash = pending.hash;
                slot.distance = distance;
                slot.state = SlotState::Occupied;
                count += 1;
                true;

            slot.hash == h && slot.key == pending.key;
                slot.value = pending.value;
                false;

            slot.distance < distance;
                swap(slot.key, pending.key);
                swap(slot.value, pending.value);
                swap(slot.distance, distance);

            index = slot_index(index + 1);
            distance += 1;

        index = slot_index(hash_of(key));
        probe = 0;
        probe < slots.size();
            slot = slots[index];
            slot.state == SlotState::Empty;
                false;

            slot.state == SlotState::Occupied && slot.key == *key;
                slot.state = SlotState::Tombstone;
                count -= 1;
                true;

            index = slot_index(index + 1);
            probe += 1;

        false;

        old = move(slots);
        slots = vector{Slot{K, V}}(capacity);
        count = 0;
        entry , old;
            entry.state == SlotState::Occupied;
                insert(entry.key, entry.value);

                entry;

        old;

        slot , slots;
            slot.state = SlotState::Empty;
            slot.distance = 0;

        count = 0;
